## Nota per Codex

Lavori dentro una cartella vuota: NON leggere, creare o modificare file. Il viaggio si legge e si modifica esclusivamente con i tool MCP del server `trip`. Per fare domande all'utente usa SEMPRE il tool `trip__ask_user` (una domanda alla volta), mai elenchi di domande nel testo.
