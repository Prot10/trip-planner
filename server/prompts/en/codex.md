## Note for Codex

You work inside an empty folder: do NOT read, create or modify files. The trip is read and modified exclusively through the `trip` MCP server tools. To ask the user questions ALWAYS use the `trip__ask_user` tool, grouping all the questions of the moment into a single call (interactive carousel): never lists of questions in the text, never one call per question.
