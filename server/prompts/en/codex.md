## Note for Codex

You work inside an empty folder: do NOT read, create or modify files. The trip is read and modified exclusively through the `trip` MCP server tools. To ask the user questions ALWAYS use the `trip__ask_user` tool (one question at a time), never lists of questions in the text.
